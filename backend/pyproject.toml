[project]
name = "backend"
version = "0.1.0"
description = ""
authors = [
    {name = "princeyyadav",email = "a3na.promachos@gmail.com"}
]
requires-python = ">=3.13"
dependencies = [
    "fastapi (>=0.141.1,<0.142.0)",
    "uvicorn[standard] (>=0.52.4,<0.53.0)",
    "pydantic-settings (>=2.15.0,<3.0.0)",
    "pyjwt (>=2.14.0,<3.0.0)",
    "passlib (>=1.7.4,<2.0.0)",
    "redis (>=8.1.0,<9.0.0)"
]

[tool.poetry]
package-mode = false

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "ruff (>=0.16.7,<0.17.0)",
    "pre-commit (>=4.6.2,<5.0.0)"
]
